# D3 Safety Module Placeholder

Expected files for D3 implementation:

- `source_pinning.py` ? allow only approved corpus sources in answers.
- `citation_verifier.py` ? check citations against document/page provenance.
- optional `tool_guard.py` ? block unsafe or out-of-scope tool calls.

Required evidence: `reports/tables/d3_safety_before_after.csv`.
